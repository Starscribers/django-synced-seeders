# Documentation requirements (fallback for systems without uv)
# Primary method: Use `uv sync --group docs` for faster installation
# Fallback method: Use this file with pip

sphinx>=5.0.0
sphinx-rtd-theme>=1.0.0
myst-parser>=0.18.0
sphinx-autodoc-typehints>=1.19.0
